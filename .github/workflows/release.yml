name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-release:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: darwin
            goarch: amd64
            exe_ext: ""
          - goos: darwin
            goarch: arm64
            exe_ext: ""
          - goos: linux
            goarch: amd64
            exe_ext: ""
          - goos: linux
            goarch: arm64
            exe_ext: ""
          - goos: windows
            goarch: amd64
            exe_ext: ".exe"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
        run: |
          set -euo pipefail
          mkdir -p dist
          name="kvartersmenyn-cli"
          outdir="dist/${GOOS}_${GOARCH}"
          out="${outdir}/${name}${{ matrix.exe_ext }}"
          mkdir -p "${outdir}"
          go build -trimpath -ldflags "-s -w" -o "${out}" .
          if [ "${GOOS}" = "windows" ]; then
            archive="dist/${name}_${GOOS}_${GOARCH}.zip"
            (cd "${outdir}" && zip -q -r "../${name}_${GOOS}_${GOARCH}.zip" .)
          else
            archive="dist/${name}_${GOOS}_${GOARCH}.tar.gz"
            tar -C "${outdir}" -czf "${archive}" .
          fi
          sha256sum "${archive}" > "${archive}.sha256"

      - name: Publish release assets
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
